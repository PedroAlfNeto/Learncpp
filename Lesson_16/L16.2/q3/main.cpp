/*
 Quizz 3:
 Define a std::vector (using an explicit template type argument) to hold the high temperature 
 (to the nearest tenth of a degree) for each day of a year (assume 365 days in a year).
*/

#include <vector>

std::vector<float> max_temp(365);