#ifndef MAIN_HPP
#define MAIN_HPP

// Print array in formatted console text
void print_array(const int array[], int length);

// Bubblesort sorter algorithm for number lists (unoptimized)
void bubblesort_unoptimized(int array[], int length);

// Bubblesort sorter algorithm for number lists (optimized)
void bubblesort_optimized(int array[], int lenght);

#endif